name: Security Scan
on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: '0 2 * * *'

env:
  GOEXPERIMENT: jsonv2

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v7
      with:
        go-version: '1.25'
    - name: Run govulncheck
      run: |
        go install golang.org/x/vuln/cmd/govulncheck@latest
        govulncheck -scan=package ./... || echo "Vulnerabilities found (see above)"
